# Results directory

The experiment writes billable-run artifacts here. Generated result files are intentionally ignored
so a partially completed or credential-specific run is not mistaken for a published result.

